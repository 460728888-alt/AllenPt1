# Allen 股票分析 1.18：线上特征对齐与净胜率优化

本次不改网站主布局，重点解决 LightGBM “训练时看到的特征”和“网站实际传入的特征”不一致的问题。

## 修复

- 行情历史不再只保留收盘价；现在按同一交易日保留收盘价、最高价、最低价和成交量。
- 修复成交量数组与日期、价格分别过滤后可能错位的问题。
- 线上量能比不再因为缺少历史成交量而长期退化为固定值 1。
- 20日和60日位置使用真实日内最高价、最低价，与训练数据计算方式保持一致。

## 新增特征

- 1、5、10、20、60日收益；
- 5日与20日波动率，以及短长波动比；
- 5/20日与5/60日平均成交量比；
- 距20日、60日高点与低点的位置；
- RSI(14)、MACD均线差相对价格、动量加速度；
- 原有财务、公告事件和正负证据计数继续保留。

训练端、Qlib备用训练端、HTTPS备用训练端和网站推理端使用同一组27个特征。

## 训练目标

旧目标主要学习未来超额收益的相对大小。新目标先区分“扣除估算往返交易成本后是否仍为正超额”，再对净赢家按收益幅度分级，使训练目标更贴近52%净胜率审批门槛。

## 安全规则不变

- 仍使用三折扩展窗口时间外验证；
- 仍默认扣除30个基点估算往返交易成本；
- 仍要求扣费后净胜率不低于52%；
- 仍要求时间外平均净超额为正且多数时间折为正；
- 5、20、60日按周期独立审批；
- 训练数据过旧或验证未通过时继续使用证据规则，不展示机器学习概率。

## 上传后操作

上传本更新包并等待 Render 部署完成，然后在 GitHub Actions 手动运行一次 **Train LightGBM with recent market data**。建议保持：

- `start_date`: `2016-01-01`
- `universe_limit`: `0`
- `transaction_cost_bps`: `30`

绿色成功仅代表训练流程完成。是否真正启用，以网站模型状态卡或 `/api/health` 中各周期的 `approved`、`active` 和扣费后胜率为准。
