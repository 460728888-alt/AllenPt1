# Allen 1.12.0 — 证据研究与个人复核

## 本次改了什么

1. 机会雷达：从均匀抽20只，改为跨股票公告扫描。可选最近1000/3000条公告、7/30/90自然日、事件类型、关键词、20交易日涨幅上限和风险偏好。
2. 按标题事件匹配后读取最多40/60家公司、每家最多3条（负面1条、正面2条）的来源正文；深入核验最多12家的价格历史、公司资料和财务，最多展示8家。上限是分层工作量控制，不是“只有8只可选股票”。所有层的实际计数在报告显示。
3. 公告来源实际发布时间与标注日期分开；读取正文计划日期及原文上下文，不从发布日期猜催化日期。不把相对期限硬转为精确日期。
4. AI得到业务、财务、历史涨幅、受益/风险/期限原文片段；输出推断和反面解释。缺AI时仍保留证据；正文失败明确标注。来源正文若多页或截断显示“部分正文”。
5. 异步扫描任务+进度查询。页面返回后可继续查看任务；服务器重启未完成任务会丢失，有数据库的已完成报告不丢失。任务最多并行2个，缓存与查询有数量/时长限制。不是持久化任务队列。
6. Allen研究卡：从报告添加跟踪、保存每次复核理由与状态、更新行情快照。研究卡按用户隔离，原始报告不改写。快照价格变化不是实盘收益或策略胜率。
7. 事件页保留股票池公告，新增研究卡计划日期时间线；投资逻辑页保留手动档案并新增研究卡；简报加入到期/失效复核。
8. 首页移除固定的行业68分，用真实净利率、利润同比和负债率替代无依据的维度分；首页技术评分明确标为技术条件评分。
9. 风险页列出具体触发条件，不再给缺数据股票默认风险分；设置页公开数据与功能缺口。原有交易计算器、持仓、研究对话、账号与通知功能保留，并非所有模块都已重写。

## 如何更新

将部署ZIP解压后的所有文件上传原GitHub仓库根目录并提交。三个雷达运行文件 opportunity.js、radar-engine.js、radar-api.js 都不能漏。
Render选择 Deploy latest commit，Live后 /api/health 版本应为1.12.0。
无需更换环境变量，无需重新建服务。有DATABASE_URL时启动会新增radar_journal表，不删除既有记录。新增公告提示自动写入。
使用原数据库前建议保留数据库备份；尚未在你的生产数据库上执行迁移。

## 数据与测试边界

- 已只读实测公开公告列表（一次取得100条）与一篇公告正文接口。这不是对生产环境全部网络、AI额度和数据库迁移的验收。
- 17项离线/本地API/UI逻辑测试通过：日期与缺失处理、扫描分层计数、账号隔离、复核追加、原始报告不变、条件保留、输入转义。测试命令：node --test opportunity.test.js radar-v2.test.js radar-ui.test.js。
- 行情抓取按源返回总数申请最多60页，每页100，限制并发4；实际有效股票会因交易状态、流动性、接口缺页而更少。行情源返回过少时不假装覆盖成功。
- 公告与正文来自东方财富现有公开接口，行情沿用项目已有新浪/Yahoo接口。可访问不代表有商业再分发授权；对外收费/扩大服务前需核查数据使用许可。
- 未接入机构一致预期、专有研报、完整行业日历、收费实时行情；没有用户许可与凭据，不会声称已接入。
- 未实现全市场连续后台监控、公告PDF全页抽取、严格样本外回测或可靠未来上涨概率。原有趋势模块未在本次完成方法学审计，不能拿其概率当保证。
- 研究卡比较两个价格快照，不含费用、分红、复权与成交约束，不代表实际收益；状态由用户根据证据复核，不由AI保证兑现。

## 发布后验收

1. 登录并选择30天/3000公告/高风险，确认进度更新、覆盖量、原文链接与数据缺失说明。
2. 加入研究卡，进入投资逻辑，追加两次复核理由；原始报告仍保持原样。
3. 另一个账号看不到上述报告/研究卡。
4. 有数据库时重启后研究卡仍在；免费实例扫描中重启则应显示任务失效并允许重试。
5. 打开事件页和简报，核对计划日期、到期复核，不把发布日期作为未来事件。
