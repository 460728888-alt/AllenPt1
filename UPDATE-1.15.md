# V1.15 Allen 模型委员会

## 新增

- Amazon Chronos-Bolt 预训练价格路径服务（独立Python服务）。
- 5、20、60日分别显示预训练模型的价格区间和上涨倾向。
- 预训练模型与原历史条件统计逐周期核验。
- 仅在两个数值模型方向一致时显示“偏强共振”或“风险共振”。
- 模型分歧、方向中性或服务不可用时明确显示继续等待。
- DeepSeek继续负责事件与公告解释，不直接改写数值预测。
- 未经训练验证的LightGBM明确不参与预测投票。

## 安全性

- Chronos输出的上涨倾向来自预测分位数插值，页面不把它冒充已校准的真实胜率。
- Python模型服务故障不会影响主网站，Node服务自动使用原有统计模型。
- 两个服务可使用相同的随机令牌进行私有调用验证。

## 上传文件

请将压缩包中的文件和目录完整上传到GitHub仓库根目录。随后按 `PREDICTOR-DEPLOY.md` 新建预测服务，并在原网站配置预测服务网址和令牌。
