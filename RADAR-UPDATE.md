# Allen股票分析 1.11.0 · 未来机会雷达（测试版）

本次仅替换原AI选股模块，保留首页、持仓、登录、用户管理等原有模块。

## 上传

解压发布包，将全部文件上传到原 GitHub 仓库根目录并提交；尤其不要漏掉新增的 opportunity.js，否则服务无法启动。无需新建服务，不要删除原来的环境变量或数据库。Render完成部署后刷新网页，侧栏应显示“未来机会雷达”。健康接口 /api/health 的版本为1.11.0。

## 已实现

- 未来7、30、90自然日窗口；保留筛选条件。
- 在现有活跃行情样本中均匀抽查最多20只的最近12条公告，降低只挑当日赢家的倾向。
- 区分窗口内计划日期线索、时间待核验线索、负面公告。
- 显示原公告链接、发布日期、标题中的计划日期、失效条件、信息缺口。
- AI解释潜在受益链与反面可能；接口不可用时仍显示证据。
- 每次结果生成新档案，按账号隔离，最近10份可重看；有DATABASE_URL时存PostgreSQL，无数据库时仅内存且重启丢失。

## 重要边界

这是事件线索筛选原型，不是预测准确率已验证的交易策略。不是全市场事件扫描，仅解析公告标题，尚未读取公告全文。公告计划日期不代表事件必然发生；合同、回购等不自动等于未来利润增长。没有日期的线索不能视为本月催化。市场预期是否已反映、历史涨幅、未来收益和胜率尚未完成验证。AI文字是推断，仍需检查原文。

既有公开数据源可能限流或不可用。界面明确报告覆盖范围和缺失量。此次测试使用离线样本，未验证生产环境外部数据、AI和PostgreSQL连通性。

## 检查

运行 node --test opportunity.test.js；node --check app.js；node --check server.js。
部署后登录两个账号，分别生成报告，确认彼此看不到对方档案。选择30天、高风险，生成后检查条件未回到默认值；重启服务后验证数据库档案仍存在。

后续完整版需要可用且有授权的未来事件日历、公告全文解析、历史行情与估值、样本外回测；这些尚未在本版完成。
