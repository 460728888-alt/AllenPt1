# V1.14 · 可验证的股票排序模型

## 本次完成

- 新增 LightGBM LambdaRank 模型加载与 Node.js 推理模块。
- 分别支持未来 5、20、60 个交易日的候选排序。
- 模型特征包含价格变化、均价结构、波动、成交量、财务变化及公告证据。
- 未来机会雷达根据选择的 7/30/90 天窗口自动对应 5/20/60 日模型。
- 新增模型状态卡，公开版本、训练日期、数据截止日、股票数量、训练样本和时间外指标。
- 模型未训练、文件缺失或验证不通过时自动回退到证据规则。
- 训练脚本严格按日期切分训练、验证和测试集，不进行随机穿越切分。
- 概率只能来自时间外测试数据的校准分箱；没有校准就不显示概率。

## 为什么当前默认显示“安全回退中”

可靠模型不能使用几只股票或当前截面临时生成，也不能使用未来才公开的财务数据训练过去。安装包没有用虚构数据制作“高胜率模型”。管理员准备好带历史时点的真实截面数据并运行训练脚本后，只有三个周期全部满足最低样本、正平均超额收益和前十分位胜率门槛，模型才会自动启用。

## 模型与其他模块的分工

1. DeepSeek：理解公告和解释可能的受益链。
2. LightGBM：在候选公司中排列研究优先级。
3. 入场规则：核验事件、价格结构、防守价和风险收益比。

排序靠前不等于可以买入，更不代表保证上涨。
