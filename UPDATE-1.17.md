# Allen 股票分析 1.17：模型验证与新数据升级

这次更新不改变网站主布局，重点修复模型可信度和启用规则。

## 新增

- 5、20、60日模型按周期独立验证和启用，不再因为一个周期失败而全部停用。
- 三折扩展窗口（walk-forward）时间外验证，严格按日期切分，避免随机切分造成未来数据泄漏。
- 默认扣除30个基点的估算往返交易成本，再计算前10%候选的净超额和净胜率。
- 数据新鲜度闸门：默认行情数据超过14天就不启用模型，避免旧模型冒充当前预测。
- 新增 `Train LightGBM with recent market data` 工作流，从近期日线数据重新训练。
- 页面明确显示每个周期是否通过、启用了哪些周期、数据截至日期和扣费后验证指标。

## 启用标准

单个周期必须同时满足：

1. 滚动时间外样本不少于10,000行；
2. 扣除估算交易成本后，前10%候选平均超额为正；
3. 扣费后胜率不低于52%；
4. 至少半数以上验证折的净超额为正；
5. 行情数据没有超过新鲜度上限。

通过一个周期只启用该周期。未通过的周期继续使用公告证据和价格规则，绝不展示伪造的机器学习概率。

## 数据限制

近期免费数据工作流仍是研究用途，不是交易所授权行情。它可能缺少退市股票，并且Qlib成分股清单在公开数据结束日期以后不保证严格的历史时点成分，因此页面必须继续展示数据来源、日期和限制。正式收费前应替换为有授权、可持续的行情与历史成分股数据源。
