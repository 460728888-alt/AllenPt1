# Allen股票分析 V1.16：可审计的 LightGBM 云端训练

本次更新把此前的“等待训练”状态变成可执行流程：

- 新增 BaoStock 沪深300＋中证500历史日线构建器；
- 新增 GitHub Actions 手动训练任务；
- 自动训练5、20、60个交易日三个横截面排序模型；
- 自动写入时间外验证结果、数据截止日、样本数和训练数据说明；
- 验证不通过时继续安全回退，不把模型分数宣传成上涨概率；
- 当前财务与公告历史特征保持中性，避免把后来才知道的信息泄漏到过去。

上传后请按 `LIGHTGBM-TRAINING.md` 操作。完整训练成功并提交模型后，Render若开启自动部署会自动更新网站。
