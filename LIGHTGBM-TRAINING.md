# Allen LightGBM 训练说明

## 数据是什么

- 股票范围：当前沪深300与中证500成分股，合并去重后约800只。
- 历史行情：当前推荐工作流使用 Yahoo 日线价格与成交量，股票范围来自 Microsoft Qlib 的沪深300与中证500清单；仅用于研究，不是交易所授权行情。
- 目标：分别学习未来5、20、60个交易日相对同日股票池中位数的超额收益排序，并优先识别扣除估算往返交易成本后仍为正的样本。
- “国信金太阳”是券商客户端；其中沪深股票与公开A股市场一致，不存在一套单独的国信历史股价。

## 为什么不会马上假装可用

训练采用三折扩展窗口时间外验证，不随机打乱。5、20、60日三个周期分别审批；一个周期通过只启用该周期，其他周期继续安全回退。单个周期必须达到：

1. 每个周期测试集不少于10,000行；
2. 模型排名前10%的样本，扣除估算交易成本后平均超额收益为正；
3. 模型排名前10%的样本，扣费后跑赢率至少52%；
4. 至少半数时间折的扣费后平均超额为正；
5. 训练数据未超过网站设置的新鲜度上限。

任何未通过的周期都继续显示“安全回退”，不会把模型分数包装成上涨概率。

## 在 GitHub 上训练

1. 将本更新包中的文件按原目录上传到仓库。
2. 打开仓库顶部 **Actions**。
3. 左侧选择 **Train LightGBM with recent market data**。
4. 点击 **Run workflow**。
5. `universe_limit` 填 `0`，`start_date` 保持 `2016-01-01`，交易成本保持 `30`。
6. 等待任务完成。工作流会把模型、验证指标和数据审计信息提交回仓库；Render开启自动部署时会随后部署。

## 当前限制

当前训练让价格、趋势、波动和成交量特征参与学习；历史财务与公告特征保持中性，因为若没有“当时的披露日期”，把今天看到的数据填回历史会产生未来数据泄漏。公开成分股清单在数据结束日期之后不保证严格的历史时点成分，且免费行情可用性不稳定，所以模型只是研究排序器，不是收益保证或自动交易信号。
