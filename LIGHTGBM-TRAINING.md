# Allen LightGBM 训练说明

## 数据是什么

- 股票范围：当前沪深300与中证500成分股，合并去重后约800只。
- 历史行情：BaoStock公开日线，默认从2018-01-01开始，前复权。
- 目标：分别学习未来5、20、60个交易日相对上证指数的超额收益排序。
- “国信金太阳”是券商客户端；其中沪深股票与公开A股市场一致，不存在一套单独的国信历史股价。

## 为什么不会马上假装可用

训练按日期切分为训练、验证、测试三段，不随机打乱。三个周期必须全部达到最低门槛，网站才会把模型标为“已启用”：

1. 每个周期测试集不少于10,000行；
2. 模型排名前10%的样本，时间外平均超额收益为正；
3. 模型排名前10%的样本，时间外跑赢率至少52%。

如果任一周期不通过，网站继续显示“安全回退”，不会把模型分数包装成上涨概率。

## 在 GitHub 上训练

1. 将本更新包中的文件按原目录上传到仓库。
2. 打开仓库顶部 **Actions**。
3. 左侧选择 **Train LightGBM rankers**。
4. 点击 **Run workflow**。
5. 第一次建议 `universe_limit` 填 `0`，从2018年开始完整训练。
6. 等待任务完成。工作流会把模型、验证指标和数据审计信息提交回仓库；Render开启自动部署时会随后部署。

## 当前限制

第一版只让价格、趋势、波动和成交量特征参与学习。历史财务与公告特征保持中性，因为若没有“当时的披露日期”，把今天看到的数据填回历史会产生未来数据泄漏。当前成分股训练还存在幸存者偏差，所以模型只是研究排序器，不是收益保证或自动交易信号。
