# V1.20 稳定机会池

这次修复“每次扫描都换一批股票”的问题。

## 新规则

- 雷达结果拆分为“本次新发现”和“持续跟踪”。
- 上一次尚未到期的候选，即使本次没有进入前八名，也继续显示。
- 有明确事件日期的候选保留到事件日后 5 天，便于核验兑现情况。
- 没有可靠事件日期的候选保留到首次报告的研究窗口结束。
- 新增反面公告时标记为“需要复核”，不会静默删除。
- 真正到期的旧逻辑退出；同一股票日后因新事件重新入选时，按新机会重新计时。
- 首次部署 V1.20 后运行雷达，会自动把最近一份旧报告迁入稳定候选池。

## 上传位置

把压缩包内文件和文件夹保持原位置，上传到 GitHub 仓库根目录并选择覆盖同名文件。`radar-lifecycle.js` 与 `radar-lifecycle.test.js` 是新增文件，也放在根目录。

Render 自动部署完成后，打开 `/api/health` 应看到版本 `1.20.0`。随后运行一次未来机会雷达，第二次扫描时即可看到“持续跟踪”。

## 数据持久性

已连接 PostgreSQL 时，系统会自动创建 `radar_lifecycle` 表，Render 重启后仍保留跟踪状态。未连接数据库时页面会明确提示，服务重启会丢失生命周期记录。
